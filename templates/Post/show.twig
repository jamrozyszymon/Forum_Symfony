{% extends "base.html.twig" %}
 {% block body %}
    <div class="container">
        <div class= "row">
            <div class = "col-6">
                <div>
                    {% include 'displayerror.twig' %}
                </div>
                
            <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Treść</th>
                </tr>
            </thead>
            <tbody class="table-group-divider">
                 {% for pagination in paginations %}
                <tr>
                <th scope="row">1</th>
                    <td>{{ pagination.content }}</td>
                    <td> 
                    {#
                        <a href="{{path('edit',{'blog':post.id})}}" class="btn btn-default btn-sm"> Edytuj </a>
                    #}

                    </td>
                </tr>
                {% endfor %}
            </tbody>
            </table>

            </div>
        </div>
    </div>

    <div class="navigation">
        {{ knp_pagination_render(paginations)}}
    </div>

 {% endblock %}